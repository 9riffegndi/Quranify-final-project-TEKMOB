name: quranify
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0   # ✅ Untuk API request
  flutter_tts: ^3.8.3   # ✅ Untuk Text-to-Speech Hadist
  shared_preferences: ^2.2.2
  audioplayers: ^5.2.1
  uuid: ^4.3.3  # For generating unique IDs
  hijri: ^3.0.0  # For Hijri date conversion
  intl: ^0.19.0  # For date formatting
  geolocator: ^14.0.2
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/   # ✅ Folder gambar/icon
    - assets/audio/    # ✅ Folder audio
